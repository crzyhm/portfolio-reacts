import React from 'react';
import SkillIcons from './SkillIcon'; // Import the SkillIcons component

const SkillGrid = () => {
  return (
    <div className="mt-5">
      <SkillIcons />
    </div>
  );
};

export default SkillGrid;
